
const Interpreter = require("./runtime/Interpreter");
const FunctionProvider = require("./runtime/FunctionProvider");
const Thread = require("./runtime/Thread");

module.exports = {
	Thread,
	Interpreter,
	FunctionProvider
};
