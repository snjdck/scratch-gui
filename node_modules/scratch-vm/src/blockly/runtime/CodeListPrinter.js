
function castCodeToString(code)
{
	if(code.length <= 1){
		return code[0];
	}
	return code[0] + "," + JSON.stringify(code.slice(1)).slice(1, -1);
}

exports.castCodeListToString = codeList => codeList.map(code => castCodeToString(code)).join("\n");